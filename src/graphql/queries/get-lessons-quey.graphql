query GetLessons($eventSlug: String) {
    event(where: {slug: $eventSlug}, stage: PUBLISHED) {
        name
        lessons(orderBy: availableAt_ASC) {
            id
            slug
            availableAt
            lessonType
            title
        }
    }
}
